**`analyze_trends()` returned `{}` for a 10-K reporting exactly two annual periods.** It gave up when no *named* period view existed, and the income-statement views require three normal durations, so a request for two periods came back empty although both were present and reachable — Auburn National reports $603,000 and $598,000 of contract revenue. A named view is a convenience rather than a precondition, so the helper now falls back to the periods the statement itself would display; filings that have a named view are unaffected. (GH #1292, bead edgartools-hsgs.3)
