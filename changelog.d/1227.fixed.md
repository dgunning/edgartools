**Markdown statement tables broke apart when a filed label contained a pipe.** `Statement.to_markdown()` emitted labels verbatim, so Global Arena Holding's "conversion of debt | shares" row carried four unescaped pipes where a two-column row has three, and Markdown parsers read it as a different shape than the header. Labels, headers and values are now escaped. (GH #1227)
