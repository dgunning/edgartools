- **bs4's three text behaviours now have one implementation each.** The lxml migration had grown seven near-identical private text helpers, one per ported file; six are folded into `edgar/documents/utils/html_utils.py` as `text_content`, `text_stripped`, `text_joined` and `text_skipping_tables`, with an `html_to_text` wrapper. Behaviour-preserving: the five characterization suites pass unchanged against their recorded bs4 baselines, 297 tests.
