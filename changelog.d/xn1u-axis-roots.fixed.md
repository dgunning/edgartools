**An axis with two domain roots kept only the first, and a childless root disappeared from the dimensional model.** `_resolve_axis_domain` processed the first `dimension-domain` arc and then broke, but XBRL Dimensions 1.0 §2.5.3 allows several roots on one axis — and a root that parents no member of its own is created nowhere else, since the domain-hierarchy pass only builds elements that parent a `domain-member` arc. Ellington Financial declares `efc_DerivativeMaturityPeriodDomain` and the childless `efc_Year2038Member` on the same axis, and the latter was absent from `domains_for_role()` although five facts use it. Every root is now registered, while `Axis.domain_id` — a single field — keeps naming the first, so no existing caller reads a different answer: across 1,040 roles in 17 filings no `domain_id` changed and exactly that one domain was recovered. This is metadata only; the facts remained queryable and no value was wrong. (GH #1278, bead edgartools-xn1u.2)
