**BDC portfolio investments carry an industry, its source and a normalized sector.** `PortfolioInvestment.industry`, `.industry_source` (`axis`, `enumeration`, `identifier`, `peer`) and `.sector`, with `PortfolioInvestments.filter(industry=)` and `.by_industry()`; the DERA path reads the same fallbacks and gains `summary_by_industry(by='sector')` and a `summary_by_company()` with fair value. Filings with a resolvable industry in 2025Q2 rise from 123 to 158 of 160. (bead edgartools-t1wr)
