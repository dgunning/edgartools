- **Quarter and date helpers moved to `edgar.dates`.** `filing_date_to_year_quarters`, `current_year_and_quarter`, `is_start_of_quarter` and `parse_acceptance_datetime` now live beside `extract_dates` instead of in `edgar.core`; `edgar.core` re-exports the same objects until 6.0 removes the shim. Continues the bounded extraction that produced `edgar/settings.py`, taking `core.py` from 579 to 514 lines.
