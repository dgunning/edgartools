**`decimals="INF"` was rewritten to integer 0 in assembled statements.** Apple's exactly-filed $0.00001 par value reached `get_statement()` claiming to be rounded to the dollar, while the same fact still read `INF` through `FactQuery`. The sentinel is now preserved; scaling and formatting ask `decimals_for_scaling` for a number, so rendered statements are unchanged. (GH #1229)
