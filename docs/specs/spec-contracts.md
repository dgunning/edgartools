# Spec Contracts (Module 0)

## Purpose

This file defines the shared contracts that all other spec modules depend on.
Every implementer subagent must read this file before reading their module spec.

## How To Use These Contracts

1. Read this file in full before opening any module spec (M2-M7).
2. Treat every rule here as normative. Module specs do not override these contracts.
3. When a module spec is silent on a topic covered here, this file wins.

Source: specification.md lines 20-32, 280-317, 1067-1118, 1467-1536

---

## Runtime Policy

- Python compatibility: `3.10+`
- All repo-authored code, tests, SQL, docs, comments, and plans must be ASCII-only.
- SEC payloads may contain non-ASCII data; that data is external and must not be
  hard-coded in repo-authored files.
- v1 runtime: Python jobs or CLI in this repo, DuckDB, Parquet, and `fsspec`.
- Supported storage backends: local filesystem, S3, Azure Blob.
- Databricks is a future-compatible target, not a v1 runtime dependency.
- Snowflake is an approved downstream gold mirror for analytics serving, not the
  canonical runtime for bronze or silver.

Source: specification.md lines 24-32

---

## Identifier Taxonomy

| Class | Fields | Notes |
|---|---|---|
| Natural business keys | `cik`, `accession_number`, `file_name` | Use for all joins |
| Surrogate keys | gold `*_key` fields only | Gold layer only |
| Generated partition keys | `cik_bucket`, `filing_year`, `accepted_date`, `fetch_date` | Warehouse-generated |
| Lineage and control keys | `sync_run_id`, `raw_object_id`, `parse_run_id` | Audit trail |

`cik_bucket` is generated by the warehouse, not inherited from the SEC, and must
never be used as a business join key.

Source: specification.md lines 280-287

---

## Tracked Universe Schema

`silver.sec_tracked_universe` is the authoritative membership table keyed by `cik`.

Required behavior:
- Seed discovery from `company_tickers_exchange.json`.
- Use `company_tickers.json` as fallback and identity support.
- Auto-enroll newly discovered SEC companies.
- Default new auto-enrolled companies to `history_mode = 'recent_only'`.
- Soft-deactivate out-of-scope companies instead of deleting them.

Recommended columns (spec says "recommended", not mandatory):

| Column | Type | Notes |
|---|---|---|
| `cik` | `BIGINT` | Primary key |
| `input_ticker` | `TEXT` | Nullable |
| `current_ticker` | `TEXT` | Nullable |
| `universe_source` | `TEXT` | `manual`, `watchlist`, `seeded_from_sec_reference` |
| `tracking_status` | `TEXT` | `active`, `paused`, `inactive` |
| `history_mode` | `TEXT` | `recent_only`, `full_history` |
| `effective_from` | `TIMESTAMPTZ` | |
| `effective_to` | `TIMESTAMPTZ` | Nullable |
| `load_priority` | `INTEGER` | Nullable |
| `scope_reason` | `TEXT` | Nullable |
| `added_at` | `TIMESTAMPTZ` | |
| `removed_at` | `TIMESTAMPTZ` | Nullable |

Source: specification.md lines 289-317

---

## Storage Path Templates

### Reference

```text
bronze/reference/sec/company_tickers/{YYYY}/{MM}/{DD}/company_tickers.json
bronze/reference/sec/company_tickers_exchange/{YYYY}/{MM}/{DD}/company_tickers_exchange.json
bronze/reference/sec/current_feed/{YYYY}/{MM}/{DD}/{HH}/current_feed_atom.xml
```

### Daily Index

```text
bronze/daily_index/sec/{YYYY}/{MM}/{DD}/form.YYYYMMDD.idx
```

### Submissions

```text
bronze/submissions/sec/cik={cik}/main/{YYYY}/{MM}/{DD}/CIK{cik_10}.json
bronze/submissions/sec/cik={cik}/pagination/{YYYY}/{MM}/{DD}/{file_name}
```

### Filing Artifacts

```text
bronze/filings/sec/cik={cik}/accession={accession_number}/index/{file_name}
bronze/filings/sec/cik={cik}/accession={accession_number}/primary/{file_name}
bronze/filings/sec/cik={cik}/accession={accession_number}/attachments/{file_name}
silver/text/sec/cik={cik}/accession={accession_number}/{text_version}.txt
gold/parser_output/{parser_name}/{parser_version}/cik={cik}/accession={accession_number}/part-*.parquet
```

Source: specification.md lines 1067-1100

---

## Source Precedence Rules

1. `company_tickers.json` and `company_tickers_exchange.json` are authoritative for
   reference snapshots and ticker-to-CIK resolution.
2. `CIK##########.json` is authoritative for current company profile,
   `filings.recent`, and the list of pagination files under `filings.files`.
3. `CIK##########-submissions-###.json` files are authoritative for the older
   filings named in the current pagination manifest.
4. Filing index pages and filing documents are authoritative for attachment-level
   detail and primary-document discovery.
5. The current-feed Atom endpoint is a low-latency signal only. It may accelerate
   discovery, but company filing metadata should be confirmed through submissions or
   filing index data before it is treated as canonical.

Source: specification.md lines 1106-1110

---

## Storage Semantics

- Bronze is immutable and append-only.
- Silver is current-state and upsertable.
- Gold is versioned by parser name and parser version.
- If the SEC changes values for an existing primary key, update the silver row in
  place and preserve the old bronze snapshots for auditability.

Source: specification.md lines 1114-1117

---

## Upsert Semantics

| Table | Rule |
|---|---|
| `sec_company` | Upserted by `cik` |
| `sec_company_ticker` | Replaced by source snapshot for the relevant reference source |
| `sec_company_address` | Upserted by `(cik, address_type)` |
| `sec_company_former_name` | Replaced for current CIK snapshot (source array is authoritative) |
| `sec_company_submission_file` | Replaced for current CIK manifest after successful manifest refresh |
| `sec_company_filing` | Upserted by `accession_number`; mutable metadata columns updated when SEC values change |
| `sec_raw_object` | Append-only; same URL may produce multiple rows over time if content changes |
| `sec_filing_attachment` | Replaced per accession when filing index is refetched successfully |
| `sec_filing_text` | Versioned by `text_version` |
| Gold parser tables | Versioned by parser version; should not overwrite prior parser outputs |

Source: specification.md lines 1467-1480

---

## Data Quality Rules

- `accession_number` must be unique in `sec_company_filing`.
- `cik` must be present for all company and filing rows.
- All bronze objects must have `sha256`.
- Every `sec_company_filing` row must map to at least one raw submissions object.
- Every parsed gold row must map to a successful `sec_parse_run`.
- Parser reruns must not overwrite old outputs without version change.
- Every tracked CIK must have one `sec_company_sync_state` row.
- Incremental sync must be able to prove why a scope was skipped, refreshed, or
  resynced using `sec_sync_run` and `sec_source_checkpoint`.

Source: specification.md lines 1498-1507

---

## Security And Compliance

- Include SEC identity on all live fetches.
- Respect SEC rate limits.
- Do not overwrite raw source artifacts.
- Retain original URLs and hashes for auditability.
- Log fetch and parse failures with enough detail for replay.

Source: specification.md lines 1529-1535

---

## Table Ownership Map

| Module | Owns These Tables |
|---|---|
| M2: Silver Metadata | `sec_tracked_universe`, `sec_company`, `sec_company_address`, `sec_company_former_name`, `sec_company_ticker`, `sec_company_submission_file`, `sec_company_filing`, `sec_current_filing_feed` |
| M3: Daily Index | `stg_daily_index_filing`, `sec_daily_index_checkpoint` |
| M4: Artifact/Text/Parser | `sec_raw_object`, `sec_filing_attachment`, `sec_filing_text`, `sec_parse_run` |
| M5: Form Parsers | `sec_ownership_reporting_owner`, `sec_ownership_non_derivative_txn`, `sec_ownership_derivative_txn`, `sec_adv_filing`, `sec_adv_office`, `sec_adv_disclosure_event`, `sec_adv_private_fund` |
| M6: Gold Star Schema | all `gold.dim_*` and `gold.fact_*` tables |
| M7: Sync Control | `sec_sync_run`, `sec_source_checkpoint`, `sec_company_sync_state`, `sec_reconcile_finding` |

---

## Status Enumerations

### checkpoint_status

`pending`, `running`, `waiting_for_publish`, `skipped_non_business_day`,
`succeeded`, `failed_retryable`, `failed_terminal`

Source: specification.md line 413

### sync_status

`queued`, `running`, `succeeded`, `failed`, `partial`, `skipped`

Source: specification.md line 1153

### tracking_status

`active`, `paused`, `inactive`

Source: specification.md line 309

### parse_run_status

`queued`, `running`, `succeeded`, `failed`, `skipped`

Source: specification.md line 943
