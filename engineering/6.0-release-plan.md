# EdgarTools 6.0 Release Plan

**Status**: Draft (2026-07-28) · target date and branching model settled 2026-08-05
**Tracking**: `edgartools-07lk` (epic) — children `07lk.1`–`07lk.4`, plus linked existing beads
**Current version**: 5.45.1
**Target release**: 2026-11-09 (rationale in *Timing*, below)

## Principles

- 6.0 is the breaking-change window: removals, namespace changes, and dependency
  floor bumps land here, each with a migration-guide entry.
- Performance work is measured, not guessed: no optimization merges without a
  before/after number from the profiling baseline.
- A Rust rewrite was evaluated (2026-07-28) and **rejected** for 6.0: the heavy
  lifting is already native (lxml/pyarrow/orjson/rapidfuzz), the hottest
  pure-Python code (table heuristics, TOC analysis) churns too fast to freeze
  into a compiled extension, and the package's pure-Python wheel + PyPy support
  is worth keeping. Revisit a *narrow, optional* PyO3 accelerator only if a
  stable hotspot survives Phase 1 vectorization.

## Workstreams

### 1. Performance — profile first

| Item | Bead | Status |
|------|------|--------|
| Profiling baseline for documents/ + xbrl/ (gates everything) | `07lk.1` | new |
| Vectorize facts.py/statements.py apply/iterrows; pyarrow-ify `read_index_file` | `07lk.2` | new |
| Nested-table XPath ~11x row/cell reprocessing (3+ hour parses) | `t6z2` | in progress |
| Unbounded colspan/rowspan memory blowup (700GB reported) | `c7kh` | in progress |
| SGML parser rewrite — 10x perf, 2.5x memory | `ceim` | in progress |
| Prospectus `related_filings` slow for large filers (JPM) | `c24m` | open |

Only 13F has documented benchmarks today (`docs-internal/analysis/thirteenf-*`);
the documents and XBRL pipelines have none. `07lk.1` fixes that, then everything
else cites its numbers. Target headline: "profiled and vectorized the XBRL and
documents pipelines — still pure Python."

### 2. Old HTML parser removal (`edgar.files`)

| Item | Bead | Status |
|------|------|--------|
| Cutover epic: edgar.files.html → edgar.documents | `436` | in progress |
| Migrate all old parser usages | `3dp` | open |
| Rebuild new-vs-legacy parity benchmark (gates removal) | `zqjn` | open |
| **Delete edgar/files + ChunkedDocument in 6.0** | `07lk.3` | new, blocked on above |

Footprint: 904K / 11 modules; 16 importing modules under `edgar/`
(`_filings.py`, `attachments.py`, `company_reports/*`, `sgml/*`,
`storage/_local.py`, …); 21 test files. Sequence: finish cutover → parity
benchmark green → deprecation warnings in late 5.x → delete in 6.0.
Removal resolves GH #870 (ChunkedDocument empty items) — verify
`edgar.documents` handles that filer-agent HTML before closing it as
fixed-by-removal.

### 3. Section extraction defects (sf1tzp audit)

External sweep of v5.43.1 ([gist](https://gist.github.com/sf1tzp/5f458bf6359f2c4a736253a6329138ce)):
**99 defects / 31,691 documents**, tracked as sub-epic `llmp.6` under the
section extraction redesign epic (`llmp`), with one child per cluster:

1. `llmp.6.1` **End-boundary overflow (40)** — missed item anchors make slices
   overshoot into the next item; the confidence-0.5 warning fires but the
   oversized slice is returned silently.
2. `llmp.6.2` **Cross-reference anchoring (12)** — anchors match mid-sentence
   cross-references or table captions at *full* confidence.
3. `llmp.6.3` **Wrong item resolution (23)** — e.g. Item 7A returning Item 7
   MD&A.
4. `llmp.6.4` **Gross over-capture (15)** — empty section maps falling back to
   megabytes of raw HTML; live extraction worse than stored (includes the 9
   stale-row findings).

Cross-cutting fixes: `llmp.6.5` whitespace-tolerant anchor probes
(`Item\xa01A.` NBSP headers — small and well-bounded, do first and re-measure)
and `llmp.6.6` honest confidence signaling instead of silent low-confidence
returns. This workstream shares root causes with `btr2`, `bpab`, `yr1z` and the
epic's Phase 4 invariant work — plan the fix inside the `llmp` redesign, not as
point patches. The audit's author was preparing upstream bug reports; consider
reaching out with the bead IDs.

### 4. httpx → httpx2 migration (GH #842, `q2iz`)

- Unblocks `httpxthrottlecache>=0.5.0` (currently capped at <0.5.0).
- Easier than originally scoped: httpx2 ships `alias_httpx`
  (pydantic/httpx2#1077), and httpxthrottlecache 0.6.0 made httpx/httpx2
  optional dependencies — paultiq suggests edgartools do the same.
- **Gating spike first**: does vcrpy (pinned <8.2) intercept httpx2? Cassette
  replay is load-bearing; if not intercepted, that's the real work.
- Exception namespace is public surface (users catch `httpx.HTTPStatusError`
  etc.) — re-export the exception types we raise through, changelog it
  prominently. Namespace break makes this a 6.0 item.

### 5. uv migration (`zc7t`)

Research task exists (CI reproducibility via uv.lock, hash-pinned resolution,
10-100x install speed, hatchling stays as build backend). Execute for 6.0:
CI workflows first, dev-environment docs second. Dovetails with `95sh`
(supply-chain hardening) and `5i4n` (Actions upgrade). No end-user impact.

### 6. Dead code sweep (`07lk.4`)

Inventory then remove: `documents/migration*.py`, deprecated legacy MCP
handlers (`company_research.py`, `financial_analysis.py`,
`industry_analysis.py`), `files/_deprecation.py` (goes with workstream 2),
`xmltools.py` (check remaining users), `thirteenf/demo_comparison.py`, and the
deprecated markers flagged across `offerings/`, `entity/`, `xbrl/`,
`thirteenf/`. Method: vulture + coverage + grep, inventory doc first, then
removal PRs.

### 7. API & dependency hygiene (added 2026-07-28)

| Item | Bead | Notes |
|------|------|-------|
| Define the public API — `__all__`, stable paths, private internals | `07lk.5` | Enabler for lazy imports and future removals; coordinate with `iirl` |
| Ship `py.typed` marker | `07lk.6` | Makes the `v7iz`/`q9tf`/`yaky` typing work visible downstream |
| Dependency diet: drop unused `tabulate`, replace **GPL** `unidecode` → `anyascii`, `textdistance` → `rapidfuzz`, retire archived `nest_asyncio` | `07lk.7` | First three are 5.x-safe; `nest_asyncio` needs Jupyter re-entrancy care |
| bs4 inventory (`07lk.8`, done) → **bs4-free in 6.0** | `07lk.11` | Scope promoted from 7.0 (2026-07-28). 32 files (14 S/13 M/5 L), ~33–38 person-days; `xmltools.py` first unlocks 12 dependents; drop `beautifulsoup4` dep in the breaking window. Expected: 5–15x on XML-parser parse steps, 2–5x DOM-heavy HTML, several-x memory (anchored on infotable_xml's measured 8–20x). See `engineering/analysis/bs4-migration-inventory.md` |
| Import-time reduction via PEP 562 lazy loading (design-first) | `07lk.9` | ~0.9s CPU today; precise measure lands in `07lk.1`; depends on `07lk.5` |
| Unified error policy + `edgar.exceptions` — no silent `None` | `07lk.10` | Folds in `q2iz` exception re-exports, `xbbp`, `llmp.6.6` pattern, `1h9c` |

**Quality bar for 6.0** (scanned 2026-07-28: ~1,100 ruff violations, 15 vulture
items): (a) one mechanical `ruff --fix` PR — ~280 auto-fixable
unused/unsorted imports and unused vars; (b) audit the **154 swallowed
exceptions** (147 `except: pass` + 7 `except: continue`) as the concrete input
to the `07lk.10` silent-failure inventory; (c) eradicate **283 `print`
statements** in library code → logging/rich console; (d) fix 29 F821 unquoted
forward refs as part of the typing push (`v7iz`/`q9tf` → `py.typed`);
(e) per-rule CI ratchet after each cleanup lands (fail on *new* T201/S110/F821)
so the debt can't regrow; (f) dead-code sweep (`07lk.4`) is import-graph +
coverage driven, not vulture — symbol-level dead code is minimal, the big
deletions arrive via `07lk.3` (edgar/files) and `07lk.11` (xmltools after bs4).
Verification-side quality stays under the existing `hqzi` epic; sequence the
docs-as-spec pass (`f1i6`) *after* the public-API declaration (`07lk.5`) so
examples verify against the 6.0 surface.

### 8. Already-planned work that belongs in 6.0

- `b7j4` — pandas 3.0 + drop Python 3.10 (in progress; classic major-release bump)
- `iirl` — cash-flow method naming consistency (breaking API cleanup)
- `5i4n` — GitHub Actions off deprecated Node 20

### 9. Structural cleanup (added 2026-07-30, epic `07lk.12`)

Four-agent structural audit of source, docs, tests, and repo root. Full detail
lives in the child beads; headline items:

| Area | Bead | Headline |
|------|------|----------|
| Source tree | `07lk.12.1` | 32 loose modules at `edgar/` root; 3-way markdown module collision; dev tooling (eval harness, training, demos) ships in the wheel — needs hatch excludes; empty `tools/`+`analysis/` packages and orphaned `xbrl/analysis/` delete-safe; `core.py` rename is high-cost (77 files) — shim if done. Coordinate with `07lk.5`. |
| Tests | `07lk.12.2` | 48% of 482 files loose at top level; markers driven by ~120 filename substrings with silent fall-through; 20 promoted reproductions to delete; 3 eval mechanisms, 2 never collected; stale `tests/legacy` config. |
| Repo bloat | `07lk.12.3` | tests/ is 2.2 GB (1.6 GB cassettes + 577 MB fixtures) + 384 MB `data/`; `.git` 829 MB. Orphan audit, then LFS vs fetch-step decision. |
| Docs boundary | `07lk.12.4` | One tracked engineering tree at `engineering/` (outside `docs/`, so it cannot reach the docs site); `docs/` is user documentation only; private local notes tree winds down now that beads owns planning; dedupe the 14 overlapping files; commit `docs/blog/` (Ghost staging); delete accidental dirs, analysis dumps at docs root, orphan images. |
| Root & scripts | `07lk.12.5` | 144+67 untracked scratch scripts need a policy; `server.json` MCP manifest stale at 5.21.1 (sync from `__about__.py` or delete); `uv.lock` held for the uv migration. |

Hygiene fix already applied (2026-07-30): generic dev-artifact ignore patterns
moved into the tracked `.gitignore`; machine-local excludes retained where
deliberate. mkdocs nav verified healthy (84/84 pages exist) and the wheel
allowlist confirmed clean of repo scratch.

### 10. Contributor experience for the AI-assisted era (added 2026-07-30)

Contribution data: last 100 merged PRs span 20+ external authors, mostly 1–4
PRs each — a drive-by pattern AI assistance will amplify. Maintainer review
attention is the scarce resource; these items protect it.

| Item | Bead | Notes |
|------|------|-------|
| AGENTS.md + CONTRIBUTING.md overhaul | `07lk.13` | Cross-tool agent instructions (DoD, hazards, test placement); fix stale doc tiers + full-suite guidance in CONTRIBUTING |
| PR template + CI gates for AI PRs | `07lk.14` | DoD checklist shapes contributor AI output; ratchets as PR gates; optional automated first-pass review |
| Public roadmap: milestone + help-wanted issues | `07lk.15` | Beads is local — distill to GitHub so contributors align instead of collide; bs4 per-file migrations are ideal PR-sized work |
| Announce reorg landing windows | `07lk.16` | Tree moves invalidate fork branches; compressed windows + path maps |
| Repo slim reprioritized P2→P1 | `07lk.12.3` | Cloud AI environments clone fresh per session; 2.2 GB is a tax on every contribution |
| Review PR #919 with cluster context | `llmp.6.7` | Pilot for the aligned-contributor flow |

## Timing (settled 2026-08-05)

**Target: 2026-11-09.** Three facts pick that week, and they agree.

*Python 3.10 reaches end of life on 2026-10-31.* Releasing after that date makes
the floor bump self-justifying — "3.10 is EOL, 6.0 requires ≥3.11" — instead of
an argument we have to make. Releasing before it means pushing users off a
runtime upstream still supports.

*pandas 3.0 has already decided the floor for us.* 3.0.0 shipped 2026-01-21
(3.0.5 as of 2026-07-22) and declares `requires-python >=3.11`. `b7j4` is
therefore one decision, not two: there is no 6.0 that takes the pandas 3.0 floor
and keeps Python 3.10.

*The tail is small and we are late, not early.* Python 3.10 is 3.8% of downloads
over the last 180 days — less than 3.14's 8.9%. Under SPEC 0, the numpy/pandas
convention of dropping a Python ~3 years after release, 3.10 was due to go
around October 2024. Treat 3.8% as an upper bound: PyPI counts include CI and
mirrors, and the real users behind it skew toward locked enterprise
environments — the cohort whose own policy moves them once 3.10 goes EOL.

No later, either: 3.11's EOL is 2027-10-31, so slipping past November reaches no
further natural boundary and buys nothing external.

## Branching and release model (settled 2026-08-05)

**No long-running 6.0 branch. Freeze releases, not commits.**

Everything lands on `main`. Phases 0 and 1 ship as 5.x, exactly as they have
been. For Phase 2, `main` goes briefly unreleasable rather than a branch going
briefly divergent.

*Why not a release branch, in this repo specifically.* Our own throughput is the
argument against it: 38 PRs merged in the 9 days after this plan opened, 13 of
them in section extraction — the subsystem Phase 2 both deletes (`07lk.3`) and
relocates (`07lk.12.1`). A long-lived branch would spend its life absorbing
conflicts from our own fix stream, and the faster `main` moves the more it
costs. The reorg is rename-heavy, and git merges renames badly when the other
side edited the moved files; the mitigation we already chose for that is
announced landing windows (`07lk.16`), which is a trunk technique. A second base
branch would also tax the contributor program (`07lk.15`) by making every
compiled help-wanted issue explain which branch to target, duplicate or drift
from the PR gate that `07lk.21` just established, and add cross-branch conflicts
across a 530 MB cassette corpus that may yet be re-recorded (`07lk.12.3`).

*The lever that makes this work is additive staging.* Most Phase 2 items have a
half that ships in 5.x under the current model, which shrinks the irreducible
window to a handful of removals and gives users a release stream that warns them
before 6.0 breaks them:

| Item | Additive half — ships in 5.x | Irreducible break |
|------|------------------------------|-------------------|
| `iirl` / `1h9c` naming | add new names, deprecate old | remove old names |
| `07lk.5` public API | declare `__all__`, ship `py.typed` | enforce privacy |
| `07lk.10` exceptions | add hierarchy, warn on silent `None` | flip returns to raises |
| `07lk.3` `edgar.files` | deprecation warnings (Phase 0) | delete package |
| `07lk.12.1` tree moves | new paths behind import shims | drop shims |
| `07lk.11` bs4 | per-file lxml migrations | drop the dependency |

`q2iz` (httpx2 exception namespace) and `b7j4` (pandas 3.0 / Python floor) are
the two that cannot be staged; they land inside the window.

*Hotfixes during the window.* If a 5.x patch is needed while `main` is
unreleasable, branch `5.x` **from the last 5.x tag** at that moment. That is a
maintenance branch carrying backports only — short-lived and cheap, the opposite
shape from a feature branch running ahead of trunk.

*The condition that would reverse this.* If the freeze window needs to run
longer than about six weeks, a branch starts paying for itself. Keep it short by
staging aggressively before October. The decision stays reversible in the cheap
direction — we can still cut a branch at freeze time if staging went worse than
expected, whereas unwinding a branch carried since August is the expensive
direction.

## Sequencing

**Phase 0 — still on 5.x (start now)**
Profiling baseline incl. import time (`07lk.1`) · vcrpy/httpx2 spike (`q2iz`) ·
parity benchmark (`zqjn`) · uv research (`zc7t`) · deprecation warnings on
`edgar.files` · dead-code inventory (`07lk.4` part 1) · bs4 inventory
(`07lk.8`) · exceptions-hierarchy design + silent-`None` inventory (`07lk.10`
spike) · lazy-import spike (`07lk.9`)

**Phase 1 — fixes that don't break anyone (ship in 5.x as ready)**
In-flight perf bugs (`t6z2`, `c7kh`, `ceim`) · vectorization (`07lk.2`) ·
section-extraction clusters (`llmp.6`) · uv in CI · drop `tabulate` / swap
`unidecode` / consolidate `textdistance` (`07lk.7` items 1–3) · `py.typed`
(`07lk.6`, once typing fixes land) · bs4→lxml migration phases 1–5 (`07lk.11` —
xmltools first, then hot paths; each PR ships with before/after numbers) ·
tests reorg + docs boundary + scripts policy (`07lk.12.2/.4/.5` — internal-only,
no user impact) · repo-bloat orphan audit (`07lk.12.3`)

**Phase 2 — the 6.0 breaking window**
Delete `edgar.files` + ChunkedDocument (`07lk.3`) · dead-code removal PRs ·
httpx2 swap with exception re-exports (`q2iz` + `07lk.10`) · pandas 3.0 / drop
3.10 (`b7j4`) · naming cleanups (`iirl` + `1h9c`) · public API declaration
(`07lk.5`) · silent-`None` conversions (`07lk.10`) · `nest_asyncio` retirement
(`07lk.7` item 4) · lazy imports if spike was clean (`07lk.9`) · drop
`beautifulsoup4` from dependencies (`07lk.11` phase 6, after `07lk.3`) ·
source-tree moves and renames (`07lk.12.1`, with `07lk.5` declaring the new
surface) · wheel excludes for dev tooling (`07lk.12.1` item 5)

**The freeze window — ~2026-10-12 to 2026-11-02**
5.x releases stop. The irreducible breaks land on `main`, which is knowingly
unreleasable for the duration. Everything that could be staged additively should
already have shipped in 5.x by the time this opens — the window's length is set
by how much did not.

**Release — 2026-11-09**
Migration guide (one entry per break) · changelog · perf numbers vs the Phase 0
baseline as the release story. Then 5.x maintenance ends and `main` resumes
normal releases as 6.x.

## Open questions

1. Does vcrpy intercept httpx2 at all? (Determines whether `q2iz` is an import
   swap or a test-infrastructure project.)
2. Should httpx/httpx2 become optional extras (paultiq's 0.6.0 pattern) or a
   hard swap?
3. Do we drop the PyPy classifier in 6.0 or keep testing it? (Relevant if a
   PyO3 accelerator is ever revisited.) Belongs with `b7j4`, which is already
   editing the same classifier block.

*Answered 2026-08-05:* ~~4. Timeline~~ — target 2026-11-09 with a freeze window
opening ~2026-10-12; see *Timing* and *Branching and release model*.
